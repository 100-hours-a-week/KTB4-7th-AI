# 맴매 AI Conventions

소상공인 매출 데이터로 예측·솔루션·챗봇을 제공하는 FastAPI 서버.
Backend가 전달한 데이터만 사용하며 **서비스 DB에 직접 접근하지 않는다.**

## 명령어

| | |
|---|---|
| 실행 | `uv run uvicorn app.main:app --host 0.0.0.0 --port 8000` |
| 테스트 | `uv run pytest` |
| 린트 | `uv run ruff check --fix . && uv run ruff format .` |
| 스텁 BE | `uv run python devtools/stub_backend.py` |
| 예측 연동 점검 | `uv run python -m devtools.forecast_check --pos <POS 엑셀>` |

## 담당 경계

- **제나**: `app/api/{solutions,insights,chat}.py`, `app/services/{solution,insight,chat}`, `app/clients/`, `app/core/`, `app/prompts/`
- **헥터**: `app/api/forecast.py`, `app/services/forecast/`

남의 영역 파일은 수정하지 않는다. 필요하면 알린다.

## API 계약

`app/schemas/` 가 코드상의 단일 진실 원천이고, 기준 문서는 **GitHub AI Wiki**다
(노션 API 정의서가 아니다 — 두 문서는 경로·응답 포맷이 서로 다르다).

- 필드 추가는 허용. 삭제·타입 변경은 버전 상향.
- 스키마를 바꾸면 BE에 먼저 알린다.
- 계약을 건드리기 전에 `contract-sync` 스킬로 위키와 대조한다.

## 설계 원칙 (위키 준수)

- LLM에는 원본 거래가 아니라 **BE가 계산한 확정 지표(metrics)만** 넘긴다.
- 계산은 코드, 해석만 모델. 프롬프트에 "직접 계산 금지"를 명시한다.
- 솔루션 카드 3장 + 인사이트는 **LLM 1회 호출**로 동시 생성한다.
- LLM 재시도는 **1회로 제한**한다. 챗봇 툴 실패는 2회까지.
- `promptVersion` 은 `app/prompts/` 파일명과 1:1 대응한다.

---

# 작업 방식

Karpathy Guidelines (MIT) 를 이 프로젝트에 맞게 축약했다.
**트레이드오프**: 속도보다 신중함 쪽으로 기운다. 사소한 작업엔 판단해서 적용한다.

## 1. 코딩 전에 생각한다

가정하지 말고, 헷갈리는 걸 숨기지 말고, 트레이드오프를 드러낸다.

- 가정은 명시한다. 확신이 없으면 **묻는다**.
- 해석이 여러 개면 전부 제시한다. 조용히 하나 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대한다.
- 불명확하면 멈춘다. 뭐가 헷갈리는지 이름 붙이고 묻는다.

이 프로젝트에서 특히: **위키와 노션 명세가 실제로 충돌한다.**
계약 관련해 애매하면 추측으로 채우지 말고 어느 문서 기준인지 되묻는다.

## 2. 단순함이 먼저다

문제를 푸는 최소한의 코드. 추측성은 전부 뺀다.

- 요청받지 않은 기능 금지. 1회용 코드에 추상화 금지.
- 요청받지 않은 "유연성"·"설정 가능성" 금지.
- 일어날 수 없는 상황의 예외 처리 금지.
- 200줄 썼는데 50줄로 되면 다시 쓴다.

기준: "시니어가 이거 과설계라고 할까?" 그렇다면 줄인다.

## 3. 수술하듯 고친다

건드려야 하는 것만 건드린다. 내가 만든 쓰레기만 치운다.

- 주변 코드·주석·포맷을 "개선"하지 않는다. 안 망가진 걸 리팩터링하지 않는다.
- 기존 스타일에 맞춘다. 내 취향이 달라도 따른다.
- 관련 없는 죽은 코드를 발견하면 **말만 하고 지우지 않는다.**
- 내 변경 때문에 안 쓰이게 된 import·변수만 제거한다.

기준: 바뀐 모든 줄이 요청으로 직접 추적돼야 한다.

## 4. 목표로 실행한다

성공 기준을 정하고 검증될 때까지 돈다.

- "검증 추가" → "잘못된 입력 테스트를 쓰고 통과시킨다"
- "버그 수정" → "재현 테스트를 쓰고 통과시킨다"
- 여러 단계면 짧은 계획을 먼저 말한다: `1. [단계] → 확인: [체크]`

약한 기준("일단 되게 해줘")은 계속 되묻게 만든다. 강한 기준은 혼자 돌 수 있게 한다.

## 코드 규칙

- 주석은 기본적으로 쓰지 않는다. **왜** 그런지가 비자명할 때만 한 줄.
- 새 의존성 추가 전에 물어본다.
- 한국어로 답한다.

## 금지

- **커밋·푸시는 사용자 승인 없이 하지 않는다.** 필요하면 요청한다.
- `dev` 에 직접 커밋하지 않는다. `feat/*` 브랜치 → `dev` PR.
  단 하나의 예외: **레포 최초 커밋.** `dev` 가 존재하지 않으면 PR을 보낼 대상이 없어서
  첫 커밋만 `dev` 를 직접 만든다 (2026-09-15). 그 이후는 전부 PR을 거친다.

## 브랜치

기본 브랜치는 `dev` 다. `main` 은 만들지 않는다 — `KTB4-7th-BE` 와 같은 구성이다.
브랜치명은 `feat/<kebab-case>`, 커밋 메시지는 Conventional Commits 영문 소문자 타입을 쓴다.

기능 작업은 GitHub Issue 생성 또는 기존 Issue 확인으로 시작한다. PR 본문에 `Closes #이슈번호`를 적어 병합 시 Issue가 자동으로 닫히게 한다.
